require('./bootstrap');

window.Vue = require('vue');

// Vue.component('compo-sample', {
//   template: '<li>{{ samp.text }}</li>',
//   props: ['samp']
// });

const app = new Vue({
    el: '#app',
    data: {
    },
    methods: {
    },
    computed: {
    }
});
